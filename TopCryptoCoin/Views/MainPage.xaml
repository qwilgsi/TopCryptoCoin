<Page x:Class="TopCryptoCoin.ViewModels.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:local="clr-namespace:TopCryptoCoin.ViewModels"
      mc:Ignorable="d"
      Background="LightGray">   

	<Grid Margin="20">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBox x:Name="SearchBox"
                 Grid.Column="0" Grid.Row="0" Height="30"
                 VerticalAlignment="Top"
                 Margin="10,10,5,0"
                 FontSize="16"
                 />

		<Button x:Name="ConfirmButton"
                Grid.Column="0"
                Content="Confirm"
                FontSize="14"
                Margin="132,50,5,24"/>

		<ListView x:Name="ListViewCoins"
                  Grid.Column="1" Grid.Row="0"
                  Grid.RowSpan="3"
                  Margin="20"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">
			<ListView.ItemTemplate>
				<DataTemplate>
					<StackPanel Orientation="Horizontal">
						<TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,5,0"/>
						<TextBlock Text="{Binding Sybol}" Foreground="Gray" Margin="0,0,5,0"/>
						<TextBlock Text="{Binding CurrentPrice, StringFormat='{}{0:C}'}"/>
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<Button x:Name="ThemeButton"
                Grid.Column="2" Grid.Row="0"
                Width="20" Height="20"
                Margin="70,10,0,70"
                HorizontalAlignment="Left"/>

		<Button x:Name="LangButton"
                Grid.Column="2" Grid.Row="0"
                Width="20" Height="20"
                Margin="0,10,0,70"
                HorizontalAlignment="Center"/>
	</Grid>
</Page>
